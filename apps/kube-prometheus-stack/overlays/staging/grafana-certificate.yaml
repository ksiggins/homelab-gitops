apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-dashboard
  namespace: monitoring-staging
spec:
  secretName: grafana-dashboard-tls
  dnsNames:
    - grafana.staging.lab.supermorphic.com
  issuerRef:
    name: letsencrypt-dns01-staging
    kind: ClusterIssuer
