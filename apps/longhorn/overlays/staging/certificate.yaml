apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-dashboard
spec:
  secretName: longhorn-dashboard-tls
  dnsNames:
    - longhorn.staging.lab.supermorphic.com
  issuerRef:
    name: letsencrypt-dns01-staging
    kind: ClusterIssuer
