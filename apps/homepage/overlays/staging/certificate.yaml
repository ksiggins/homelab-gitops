apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-dasbhoard-certificate
spec:
  secretName: homepage-dashboard-tls
  dnsNames:
    - homepage.staging.lab.supermorphic.com
  issuerRef:
    name: letsencrypt-dns01-staging
    kind: ClusterIssuer
